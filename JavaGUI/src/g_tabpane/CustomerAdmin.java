package g_tabpane;

import java.awt.Color;

import javax.swing.JPanel;

public class CustomerAdmin extends JPanel {

	public CustomerAdmin() {
		// 색깔 만드는법? Color : r(red) + g(green) + b(blue) 를 이용해서 색상을 만든다.
		setBackground(new Color(200, 150, 255));
	}
}
